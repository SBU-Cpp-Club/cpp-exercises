

int main()
{
   
    // a) is not valid because it cannot have the reserved word double
    // b) is not valid because it contains - 
    // d) is not valid because it has to start with letter or _

    //here are the valid ones	
    double Double = 3.14;
    int _;

    return 0;

}
